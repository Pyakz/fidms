{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "build": "tsc",
    "dev": "bun run db:up && bun --watch run src/index.ts && tsc --watch",
    "studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "@hono/sentry": "^1.2.2",
    "better-auth": "^1.3.25",
    "drizzle-orm": "^0.44.6",
    "hono": "^4.7.11",
    "pg": "^8.16.3",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.15.5",
    "drizzle-kit": "^0.31.5",
    "tsx": "^4.20.6"
  }
}
